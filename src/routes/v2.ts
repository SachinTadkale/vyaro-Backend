/**
 * Module: API v2 Router Placeholder
 * Purpose: Reserved for future backend versioning without breaking v1 contracts.
 */
import { Router } from "express";

const apiV2Router = Router();

export default apiV2Router;
