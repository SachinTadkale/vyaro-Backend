/**
 * Module: Prisma
 * Purpose: Implements the Prisma module for FarmZy.
 * Note: Documentation-only change; behavior remains unchanged.
 */
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

export default prisma;
